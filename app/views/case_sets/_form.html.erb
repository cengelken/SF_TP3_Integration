<%= form_for(@case_set) do |f| %>
  <% if @case_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case_set.errors.count, "error") %> prohibited this case_set from being saved:</h2>

      <ul>
      <% @case_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
